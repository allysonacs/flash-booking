/**
 * Camada de apresentação (HTTP).
 *
 * <p>Responsabilidades: traduzir requisições HTTP em chamadas de serviço, validar o
 * formato da entrada e serializar a resposta. Controllers são finos — não contêm regra
 * de negócio, não acessam repositórios e não gerenciam transações.
 */
package com.example.flashbooking.controller;
