/**
 * Contratos de entrada e saída da API.
 *
 * <p>Implementados como {@code record}s imutáveis, separados das entidades para que o
 * contrato público possa evoluir independentemente do modelo de persistência.
 */
package com.example.flashbooking.dto;
