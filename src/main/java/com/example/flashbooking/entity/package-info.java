/**
 * Modelo de domínio persistente (entidades JPA).
 *
 * <p>Entidades não são expostas diretamente pela API: a conversão para DTOs ocorre na
 * fronteira do controller, evitando acoplar o contrato HTTP ao schema do banco.
 */
package com.example.flashbooking.entity;
